package com.ecommerce.product_service.infrastructure.kafka.topics;

import org.springframework.context.annotation.Configuration;

@Configuration
public class KafkaTopicConfig {

    /*
    @Bean
    public NewTopic toProductCreatedEventTopic() {
        return TopicBuilder.name("product-created-events")
                    .partitions(3)
                    .replicas(1)
                    .build();
    }
                    */
}
